import { FormulariosTipos } from './formularios-tipos.model';

describe('FormulariosTipos', () => {
  it('should create an instance', () => {
    expect(new FormulariosTipos()).toBeTruthy();
  });
});
